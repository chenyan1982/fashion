fashion:
  image: fashion:latest
  command: npm run dev
  ports:
  - 3000:3000
  - '22'
  volumes_from:
  - smart-store
smart-store:
  image: smart-store:latest
  volumes_from:
  - node-modules
node-modules:
  image: node-modules:latest
  privileged: false
  restart: always
